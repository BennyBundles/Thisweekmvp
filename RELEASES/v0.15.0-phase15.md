# Release Record — v0.15.0-phase15

**Release:** `v0.15.0-phase15`  
**Production branch:** `main`  
**Validated release commit:** `8caca1c864c0608ce808210605a0b03eb1779655`  
**Stable milestone branch:** `stable/v0.15.0-phase15`  
**Rollback commit:** `351a052ba214d4813b296b4cd3dc7965e695b6e1`  
**Rollback branch:** `rollback/phase15-pre-release-2026-09-18`  
**GitHub Actions run:** `35410845516`  
**Workflow conclusion:** success

## Automated smoke result

The release workflow completed both jobs successfully.

### Validate job
- Checkout production commit — passed
- Node setup — passed
- `phase0-static-check.mjs` — passed
- `release-smoke-check.mjs` — passed

### Deploy job
- Checkout validated production commit — passed
- Stage production site — passed
- Configure Pages — passed
- Upload Pages artifact — passed
- Deploy to GitHub Pages — passed
- Post-deployment release verification — passed

## Production verification

Cache-busted production URL:

`https://bennybundles.github.io/Thisweekmvp/?v=8caca1c8`

Release manifest:

`https://bennybundles.github.io/Thisweekmvp/release.json?v=8caca1c8`

The workflow's post-deploy verification required the production HTML to contain the application marker and the deployed release manifest to contain the exact release commit.

## Release notes

Phase 15 establishes repeatable production release discipline:

- `main` is the production branch.
- Static regression tests run before deployment.
- Release smoke tests run before deployment.
- Production is staged into a dedicated `_site` artifact.
- Each deployment generates a `release.json` manifest.
- Each deployment is checked after GitHub Pages publishes it.
- Cache-busted URLs are the verification path.
- Changelog, release policy, release checklist, rollback references, and state-migration notes are required release records.
- The production head that existed before Phase 15 is preserved as a rollback branch.

## State migration

No new persistent financial-state migration was introduced by Phase 15.

- Core schema: v3
- Portable schema: v1
- Phase 13 migrations remain authoritative.

## Trust baseline

The release retains the Phase 14 browser-local privacy/security baseline.

It does not enable:

- cloud sync
- backend financial storage
- live bank credentials
- live bank-token connections

## Stable milestone note

The connected GitHub write surface used during this build does not expose Git tag creation. The repository therefore preserves the stable milestone using:

- exact immutable commit SHA in this release record; and
- dedicated `stable/v0.15.0-phase15` branch.

If an immutable Git tag is created later, it should point to the validated release commit above and must not replace this release record.

## Later production commits

Commits after this milestone may contain later phases. They do not change the known Phase 15 release point recorded here.
